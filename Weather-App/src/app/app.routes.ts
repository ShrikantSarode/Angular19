import { Routes } from '@angular/router';
import { WeatherHomeComponent } from './weather-home/weather-home.component';

export const routes: Routes = [{ path: '', component: WeatherHomeComponent }];
